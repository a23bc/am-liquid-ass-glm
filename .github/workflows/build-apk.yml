name: Build Release APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      ANDROID_HOME: /usr/local/lib/android/sdk

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Ensure Android SDK packages
        run: |
          # backdrop 2.0.1 transitively pulls androidx.compose 1.12.0,
          # which requires compileSdk 37 + build-tools 37.0.0.
          # ubuntu-latest doesn't ship these pre-installed, so we
          # install them via sdkmanager directly. We deliberately avoid
          # android-actions/setup-android@v3 — its multiline `packages:`
          # parser mishandles the input on cmdline-tools/16.0.
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          if [ ! -x "$SDKMANAGER" ]; then
            SDKMANAGER="$(ls "$ANDROID_HOME"/cmdline-tools/*/bin/sdkmanager 2>/dev/null | head -1)"
          fi
          echo "Using sdkmanager: $SDKMANAGER"
          yes | "$SDKMANAGER" --licenses > /dev/null 2>&1 || true
          for pkg in "platforms;android-37" "build-tools;37.0.0" "platform-tools"; do
            "$SDKMANAGER" --install "$pkg" || echo "ignored: $pkg (likely already installed)"
          done
          "$SDKMANAGER" --list_installed || true

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build release APK
        run: ./gradlew :app:assembleRelease --no-daemon --stacktrace

      - name: Locate built APK
        run: |
          echo "Searching for built APK..."
          find app/build/outputs -name "*.apk" -type f || true

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: amliquidass-release-apk
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: error
          retention-days: 30
